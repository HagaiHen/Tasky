const styles = {
  menuItem: {
    height: '50px',
    display: 'flex',
    alignItems: 'center',
    borderRadius: 'var(--border-radius)',
    padding: '0.5rem',
    cursor: 'pointer',
    textDecoration: 'none',
    color: 'inherit',
    // For hover, you'll need to handle this dynamically (shown below)
  }
};

export default styles;