const styles = {
  navbar: {
    height: 'var(--nav-size)',
    backgroundColor: 'var(--bg)',
    padding: '0 1rem',
    borderRadius: 'var(--border)'
  },
  navbarNav: {
    maxWidth: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'flex-end'
  }
};

export default styles;